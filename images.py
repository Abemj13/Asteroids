
import pygame
import os

# Path to your image (adjust this if your folder is different)
IMAGE_PATH = os.path.join("images", "space background.jpg")

# Load the image only once
background = pygame.image.load(IMAGE_PATH)
